* `equ` gives a name to a value, and the name goes in the first column.
    equ 12
* `section` with a number needs no name; `section` on its own is naming the
* section in force, and there is nothing to name it.
    section

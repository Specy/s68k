* `fail` is the program's own error, and its message is the rest of the line,
* commas and spaces included (Directives/fail.htm).
    fail ERROR, Argument missing in call to foo macro.
* Without a message it is EASy68K's default.
    fail
